{
  "name": "cyclops_shared",
  "version": "2.0.0",
  "description": "Cyclops shared protocol + UI state machine (host/arduino/xiao)",
  "build": {
    "srcDir": "src",
    "includeDir": "include",
    "flags": ["-Iinclude"]
  }
}
