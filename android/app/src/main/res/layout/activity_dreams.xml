<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Proposals"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/cyclops_primary" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Cyclops reviews your notes and progress in the background and suggests next actions here."
        android:textSize="13sp"
        android:textColor="@color/cyclops_secondary" />

    <Button
        style="@style/Widget.Cyclops.Button.Primary"
        android:id="@+id/btnReview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="Review now" />

    <LinearLayout
        android:id="@+id/dreamList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="12dp"
        android:orientation="vertical" />

    <TextView
        android:id="@+id/dreamEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="16dp"
        android:text="No proposals right now.\nTap Review now, or let the background loop run."
        android:textColor="@color/cyclops_secondary" />
</LinearLayout>
