<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- One-line orientation so the screen isn't a wall of fields. -->
        <TextView
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:text="Only Connection is required — tap Find brain, or paste your server URL. Everything below is optional."
            android:textSize="13sp"
            android:textColor="@color/cyclops_secondary"
            android:layout_marginBottom="4dp" />

        <TextView style="@style/SettingsHeader"
            android:id="@+id/hdrConnection"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Connection" />

        <!-- Find brain FIRST — it's the zero-typing path. -->
        <Button
            style="@style/Widget.Cyclops.Button.Primary"
            android:id="@+id/btnFind"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:text="Find brain on this network"
            android:contentDescription="Auto-discover the brain server via LAN broadcast" />

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/edUrl"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:hint="…or paste the URL (http://host:8080)"
            android:inputType="textUri" />

        <LinearLayout
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="horizontal" android:gravity="center_vertical"
            android:layout_marginTop="8dp">
            <TextView
                android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1" android:text="Transport to wearable"
                android:textColor="@color/cyclops_secondary" />
            <Spinner
                android:id="@+id/spTransport"
                android:layout_width="wrap_content" android:layout_height="wrap_content" />
        </LinearLayout>

        <TextView style="@style/SettingsHeader"
            android:id="@+id/hdrModel"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Model" />

        <!-- Provider is a pick-list, not a string to memorize. -->
        <TextView
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Provider"
            android:textColor="@color/cyclops_secondary"
            android:layout_marginTop="4dp" />
        <Spinner
            android:id="@+id/spProvider"
            android:layout_width="match_parent" android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/lnkGetKey"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:padding="6dp"
            android:text="Get an API key ↗"
            android:textColor="@color/cyclops_primary"
            android:contentDescription="Open the provider's API-key page in a browser" />

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/edApiKey"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:hint="API key (stored on device only)"
            android:inputType="textPassword" />

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/edLocalEndpoint"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:hint="Endpoint (auto-filled for local providers)"
            android:inputType="textUri" />

        <TextView style="@style/SettingsHeader"
            android:id="@+id/hdrPersona"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Persona" />

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/edPersonaName"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:hint="Name (how the agent identifies)" />

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/edPersonaVoice"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:hint="Voice (terse / friendly / formal / …)" />

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/edPersonaBio"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:hint="Bio (who it is, role, constraints)"
            android:inputType="textMultiLine" android:minLines="2" />

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/edPersonaNote"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:hint="System note (extra instructions)"
            android:inputType="textMultiLine" android:minLines="2" />

        <!-- Advanced per-tool overrides: 10 jargon fields most users never
             touch. Collapsed behind a disclosure so Settings reads clean. -->
        <TextView
            android:id="@+id/hdrTools"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:padding="6dp"
            android:layout_marginTop="14dp"
            android:text="▸ Per-tool model overrides (advanced)"
            android:textStyle="bold"
            android:textColor="@color/cyclops_primary"
            android:contentDescription="Show or hide per-tool provider and model overrides" />

        <!-- rows are added programmatically: one provider+model pair per tool -->
        <LinearLayout
            android:id="@+id/toolRows"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone" />

        <Button
            style="@style/Widget.Cyclops.Button.Primary"
            android:id="@+id/btnSave"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Save"
            android:contentDescription="Save settings and push profile to the brain" />
    </LinearLayout>
</ScrollView>
